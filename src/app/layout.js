import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Medliw - Trusted Care for Life",
  description: "Compassionate healthcare delivered by professionals who care. Comprehensive medical services including vaccination, health screening, chronic disease management, and urgent care.",
  keywords: "medical clinic, healthcare, vaccination, health screening, chronic disease management, urgent care, family doctor",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
